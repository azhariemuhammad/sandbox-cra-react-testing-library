import React from 'react'
import Hello from '../components/Hello'

test('render username', () => {
  let usernameMocks = 'azharie'
  // render Hello component with props usernameMocks
  // use querySelector to find elemant with class greeting
  // expect greeting element toHaveTextContent = usernameMocks
})
